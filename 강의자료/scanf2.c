#include <stdio.h>

int main()
{
	char ch;
	short int n;
	int n2;
	long int n3; 
	float fnum;
	double dnum;

    printf("input char ? ");
	scanf();

	printf("input short int ? ");
	scanf();

	printf("input  int ? ");
	scanf();

	printf("input long  int ? ");
	scanf();
	
    printf("input float ? ");
	scanf();

    printf("input double ? ");
	scanf(");

	printf("%c, %hd, %d, %ld, %f,  %lf \n",
		              ch, n1, n2, n3, fnum, dnum );

	return 0 ;
}
